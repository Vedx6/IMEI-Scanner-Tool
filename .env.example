# Example environment variables for local development
# Create a local `config.js` (do NOT commit it) using the value below.

# The paid API URL expected by the app. It should be the base URL up to the IMEI parameter.
# Example (replace with your actual paid API endpoint/base):
# PAID_API_URL=https://ng-imei-info.vercel.app/?api_key=YOUR_KEY&imei_num=

PAID_API_URL=

---
# To create config.js from this file, create `config.js` containing:
// config.js (do NOT commit)
// window.env = window.env || {};
// window.env.PAID_API_URL = 'https://ng-imei-info.vercel.app/?api_key=YOUR_KEY&imei_num=';
